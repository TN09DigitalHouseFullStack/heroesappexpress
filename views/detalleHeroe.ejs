<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %> 
<body>
    <%- include('./partials/header.ejs') %> 
    <main>

        <h2>Detalle <%= heroe.superhero %> </h2>

        <article class="detalle-article">
            <img src="/images/<%= heroe.id %>.jpg" alt="Imagen de <%= heroe.superhero %> ">
            <ul>
                <li><%= heroe.publisher %> </li>
                <li><%= heroe.alter_ego %> </li>
                <li><%= heroe.first_appearance %> </li>
                <li><%= heroe.characters %> </li>
                <h3> Powerstats </h3>
                <% Object.keys(heroe.powerstats).forEach(stats => { %>
                
                    <li><%= stats %>: <%= heroe.powerstats[stats] %> </li>
                <% }); %>
            </ul>
        </article>
        <button onclick="history.back()" class="btn"><i class="fa fa-arrow-left"></i> back</button>
    </main>
</body>
</html>